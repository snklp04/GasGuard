{
  "buildCommand": "cd gasguard_frontend && npm install && npm run build",
  "outputDirectory": "gasguard_frontend/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
